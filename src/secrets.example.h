#pragma once

// Copy this file to src/secrets.h for local USB flashes.
// src/secrets.h is ignored by git so Wi-Fi credentials are not published.
static const char *DEFAULT_WIFI_SSID = "";
static const char *DEFAULT_WIFI_PASSWORD = "";
